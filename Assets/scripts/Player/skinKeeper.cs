using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class skinKeeper : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    //אולי לנסות שאם מספר הסצנה משתנה אז להגיד שהסקין שווה לסקין שהיה בסצנה הקודמת
}
